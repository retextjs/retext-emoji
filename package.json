{
  "name": "retext-emoji-www",
  "private": true,
  "dependencies": {
    "retext": "^6.0.1",
    "retext-emoji": "^5.0.1"
  },
  "devDependencies": {
    "browserify": "^13.1.1"
  },
  "scripts": {
    "build": "browserify index.js > build.js"
  }
}
